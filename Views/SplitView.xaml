<UserControl x:Class="GHSMarkdownEditor.Views.SplitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GHSMarkdownEditor.Views">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="editorColumn" Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition x:Name="previewColumn" Width="*"/>
        </Grid.ColumnDefinitions>

        <local:EditorView x:Name="editorView" Grid.Column="0"/>

        <GridSplitter x:Name="gridSplitter"
                      Grid.Column="1"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="{DynamicResource MaterialDesign.Brush.Primary.Light}"
                      DragCompleted="GridSplitter_DragCompleted"/>

        <local:PreviewView x:Name="previewView" Grid.Column="2"/>
    </Grid>
</UserControl>
