<UserControl x:Class="GHSMarkdownEditor.Controls.ConfirmCloseDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <StackPanel Margin="16" MinWidth="300">
        <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0 0 0 8">Unsaved Changes</TextBlock>
        <TextBlock x:Name="MessageText"
                   Style="{StaticResource MaterialDesignBody1TextBlock}"
                   TextWrapping="Wrap"
                   Margin="0 0 0 16"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="{x:Static sys:Boolean.FalseString}"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    Margin="0 0 8 0">CANCEL</Button>
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="{x:Static sys:Boolean.TrueString}"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">CLOSE WITHOUT SAVING</Button>
        </StackPanel>
    </StackPanel>
</UserControl>
