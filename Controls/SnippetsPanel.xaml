<UserControl x:Class="GHSMarkdownEditor.Controls.SnippetsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:models="clr-namespace:GHSMarkdownEditor.Models">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource MaterialDesign.Brush.Primary}"
                Padding="12 6">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Command="{Binding StartAddSnippetCommand}"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Width="24" Height="24" Padding="0"
                        ToolTip="Add snippet"
                        Foreground="{DynamicResource MaterialDesign.Brush.Primary.Foreground}">
                    <materialDesign:PackIcon Kind="Plus" Width="14" Height="14"/>
                </Button>
                <TextBlock Text="Snippets"
                           FontSize="12" FontWeight="SemiBold"
                           Foreground="{DynamicResource MaterialDesign.Brush.Primary.Foreground}"
                           VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- Add form -->
        <Border Grid.Row="1"
                Padding="8"
                Background="{DynamicResource MaterialDesign.Brush.Card.Background}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAddingSnippet}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel>
                <TextBox Text="{Binding NewSnippetName, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Name"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         FontSize="12" Margin="0 0 0 6"/>
                <TextBox Text="{Binding NewSnippetContent, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Content"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         FontSize="12" AcceptsReturn="True"
                         MinLines="3" MaxLines="5"
                         TextWrapping="Wrap"
                         Margin="0 0 0 8"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel"
                            Command="{Binding CancelAddSnippetCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            FontSize="11" Height="28" Padding="8 0" Margin="0 0 4 0"/>
                    <Button Content="Save"
                            Command="{Binding SaveNewSnippetCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            FontSize="11" Height="28" Padding="8 0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Snippet list -->
        <Grid Grid.Row="2">
            <!-- Empty state -->
            <TextBlock Text="No snippets yet."
                       FontSize="12" Opacity="0.55"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Snippets.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ListBox ItemsSource="{Binding Snippets}"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     Background="Transparent">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:SnippetItem}">
                        <Button Command="{Binding DataContext.InsertSnippetCommand,
                                         RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                Padding="8 4"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                ToolTip="{Binding Preview}">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Delete"
                                              Command="{Binding PlacementTarget.Tag,
                                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding}"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                            <Button.Tag>
                                <Binding Path="DataContext.DeleteSnippetCommand"
                                         RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </Button.Tag>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}"
                                           FontSize="12" FontWeight="Medium"/>
                                <TextBlock Text="{Binding Preview}"
                                           FontSize="11" Opacity="0.6"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
